interface:
  display_name: "GPU PMC Sweep"
  short_description: "采集 NVIDIA、Adreno 与 Mali GPU 的 PMC 和独立 latency"
  default_prompt: "使用 $gpu-pmu-sweep 按冻结 manifest 采集 GPU PMC、availability、selection plan 和独立 latency。"
