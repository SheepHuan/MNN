# OrangePi 2026-07-03 01

## Summary

- Objective: optimize OpenCL PIC no-repair / decode-repair attention by keeping PagedCache as the source of truth while adding a decode-friendly transposed K view.
- Implemented `SharedPagedCache::decodeKey` with layout `[B, H_kv, D, max_slots]`, allocated together with PagedCache instead of during decode.
- Added one-time `decode_prepare_transpose_k` from row-layout PagedCache K to DecodeK. This is profiled separately and is not counted as decode TPOT in the A/B table.
- Added q=1 fused DecodeK append+attention path and q>1 sparse decode-repair path. q>1 uses a separate sparse append/update kernel, then a DecodeK attention kernel, because repair rows need a kernel-launch boundary before later rows read their updated K/V.
- Fixed PagedCache reuse to accept `maxSlots >= required` so decode does not recreate layer cache when the request shape moves between 4096 and 4608 slots.
- Built OrangePi artifact and ran Qwen3-4B, Llama3.2-3B, and MiniCPM5-1B `ctx512`, `q=1/2/4/6/8`, old row-PagedCache path vs new DecodeK path.
- Result: DecodeK is not a universal default yet. It is clearly faster for Llama q=1/2/4/6/8 and MiniCPM q=2/4/6, modest/mixed for Qwen. Highest-performance serving should select per model/shape/q or keep the path env-gated until an automatic heuristic is added.
- The implemented q>1 kernel is a sparse-row DecodeK kernel, not yet the true q2/q4/q8 small-Q tile kernel with shared K-load across rows from the original target note. That remains the next optimization if the goal is to improve Qwen q=2/q8 and reduce per-row duplicate K reads.
- Follow-up P2 implementation added a q=1 append+readonly micro path behind `MNN_PAGED_ATTENTION_DECODE_TRANSPOSED_K_ATTENTION_ONLY=1`. It separates historical DecodeK prepare, current-token append, readonly attention, and optional rank capture in profile output. Default production routing is unchanged.
- Follow-up P1 hardening records a host-side slot prefix snapshot for DecodeK readiness. Decode now only accepts ready-hit/prefix-stable DecodeK and will not launch `decode_prepare_transpose_k` with `inside_decode=1`.
- A pure q=1 readonly-only micro gate, `MNN_PAGED_ATTENTION_DECODE_TRANSPOSED_K_READONLY_ONLY=1`, runs only when DecodeK already covers `kv_len`; it does not append K/V or prepare in decode.
- Local compile smoke passed for both Rhino/Aidlux and OrangePi OpenCL `MNN_CL` targets.
- Consolidated plan added: keep q=1 default conservative until Rhino fused-vs-append record data is complete; implement q>1 qtile after the two-stage sparse append boundary; promote default routing only after no-regression TPOT across the three target models, both devices, and q=1/2/4/6/8.
- Rhino warmed-cache q=1 record smoke completed for MiniCPM5-1B, Qwen3-4B, and Llama3.2-3B. Result: old/fused/append+readonly are effectively tied on MiniCPM and Llama, while Qwen slightly prefers append+readonly. This supports keeping q=1 routing conservative and device/shape scoped instead of promoting DecodeK globally.
- Warmed Rhino logs were clean: no `ERROR`, no `Cache invalid`, no `target unavailable`, no `decode_prepare_transpose_k`, and no `decode_prepare_inside_decode=1`.
- q=1 hardening follow-up: default `MNN_PAGED_ATTENTION_DECODE_TRANSPOSED_K=1` no longer auto-routes q=1 to DecodeK fused or append+readonly. q=1 DecodeK variants are now explicit opt-in via `MNN_PAGED_ATTENTION_DECODE_TRANSPOSED_K_Q1_ROUTE=fused|append_readonly` or the readonly/attention-only debug flags.
- Added `pic_decode_repair_tokens_per_step` so non-decode setup only prepares DecodeK when actual q>1 decode-repair is active; no-repair q=1 keeps prepare out. A debug Rhino Llama q=1 run confirmed no `decode_key prepare` and no q1 transposed-K route branch, only the old `copy_paged_kv` invalidate.
- route6 q=1 median smoke (`ctx512`, `max_tokens=8`, warm=2, repeats=5) is dominated by device variance on OrangePi, but it validates the control-plane contract: prepare is absent from timed decode and q=1 production remains old identity unless explicitly forced. The optimization focus now moves to q>1 qtile after sparse DecodeK append.
- Added Decode Optimization Plan V2: q=1 old fallback is only a guardrail, not success. Next steps are true old/new attention-only q=1 profiling, then K-vectorized or GQA-cooperative DecodeK q=1 kernels if attention-only loses, then q>1 qtile kernels after the existing sparse append boundary.
- Added Decode Optimization Plan V3 after q=1 new attention-only smoke exposed a harder boundary bug: first decode was still expanding `max_slots=4096 -> 4608` and lazily hydrating a 512-token persistent source segment. The plan now requires an explicit full-reuse decode prepare stage before timed decode: reserve request/source capacity, hydrate all layers, run `decode_prepare_transpose_k` for the logical prefix, then decode may only append current token K/V/DecodeK and run attention/rank.
- Final q=1 route hardening: `preparePagedDecode()` is now called before server decode, and q=1 auto routing is conservative by device/model. Mali only routes Llama3.2-3B to append+readonly DecodeK; MiniCPM5-1B and Qwen3-4B stay on old identity. Adreno routes Llama3.2-3B and Qwen3-4B to fused DecodeK; MiniCPM5-1B stays old. Rebuilt and synced OrangePi/Rhino artifacts after this selector change.
- Final q=1 `ctx512/max_tokens=16/repeats=3` TPOT after hardening: OrangePi Llama 207.574 ms, MiniCPM 50.267 ms, Qwen 237.219 ms; Rhino Llama 75.948 ms, MiniCPM 26.466 ms, Qwen 96.190 ms. No failure CSV was produced.
- Current q>1 decode-repair still uses two-stage sparse append plus per-row DecodeK attention. The next real optimization is a qtile decode attention kernel for q=2/4/8, with q=6 using q8 masking, so one workgroup/tile shares the same historical K load across multiple active Q rows.
- Profile field parity was completed for old identity fused-KV decode. Rebuilt and synced OrangePi/Rhino artifacts, then ran `decode_profile_fields_prepare_smoke_v2_20260703`: 12 ok rows, no `failures.csv`; all six device/model logs show `prepare_in_timed_decode=0`, `prepare_inside_decode=0`, and `missing_required_fields=0`. This locks P0: full historical DecodeK prepare is outside timed decode, while decode attention lines expose `prepare_us`, `append_us`, `attention_us`, `rank_us`, `lane`, `record_queue`, `slot_identity`, and `decode_key_ready_hit`.
- Final route-v2 after endpoint TPOT A/B: q1 stays conservative; Mali qtile is limited to Llama 24/8 q2/q4 and Qwen 32/8 was disabled after a profile smoke proved q4 entered qtile but regressed. A post-patch Qwen q4 smoke confirmed `qtile=0` and `decode_prepare_inside_decode=0`.
- Same-window OrangePi A/B with the patched artifact shows new route-v2 wins 11/15 rows, but still regresses Llama q4, MiniCPM q4, Qwen q1, and Qwen q4. Rhino final wins 14/15 rows, with only MiniCPM q1 slower. Conclusion: the control-plane fixes are valid, but default production promotion still requires more kernel/detail work; keep the route env-gated or shape-tuned.
- Final implementation target from here: keep prepare outside decode and preallocate DecodeK, then make q=1 launch/record overhead match old identity and make q>1 a true shared-K qtile kernel. If attention-only does not win, tune lane/local-memory/K-load shape before expanding routing.
- Added `claude_handoff_decode.md` as the current handoff note: no old-route convergence, final synced artifact state, current q1/q>1 results, failed q1 direct-read experiment, Mali q>1 lane64 default, and next qtile/profile steps.
- V6 execution started (plan `/root/.claude/plans/refactored-munching-wren.md`, workflow ref `references/decode_opencl_workflow.md`). Step 0.5: wrote `references/decode_opencl_workflow.md` (build/sync/TPOT A/B/P0 scan) + CLAUDE.md pointer. Step 0: rebuilt route-v2 OrangePi artifact from `ca9fd3e`, confirmed P0 clean, reproduced the baseline A/B table (Qwen q1 +20.17ms, MiniCPM q4 +44.68ms, Llama q4 +14.75ms, Qwen q4 +17.25ms REG). Step 1: implemented q=1 GQA-cooperative transposed-K kernel (`decode_causal_attention_hd128_transposed_k_fused_kv_gqa_row{32,64,128}`, env `MNN_PAGED_ATTENTION_DECODE_GQA_FUSED=1`). Result: Llama q1 fixed (232→204ms, 1.152x) but MiniCPM q1 (kv_heads=2 → only 2 workgroups, occupancy collapse) and Qwen q1 regressed; lane sweep 32/64/128 cannot recover either. Step 1 not promoted to default (kept env-gated). q1 needs V-side/overhead axis, not K-share. GQA kernel kept for Step 4 qtile×GQA. Detailed table and rank-capture/lane/TPOT notes in `context.md` "V6 Step 1".
- Analysis: why normal LLM decode stays fast reusing long-context KV (contiguous GPU buffer, no slot_table indirection, record-queue replay, no per-step prepare/score/rank) vs PIC decode overhead — see `context.md` "V6 Step 1" tail and the Q-truncation/slot_table investigation.
- Analysis: `slot_table` is always identity (`request_base==0`, `slot_table_host[i]=i`); removing it has zero functional impact — prefill Q-truncation uses `sparse_query` (not slot_table), persistent-source hydrate uses kernel args (`_picCacheSourceSlotBase`, not slot_table). See `references/paged_cache_slot_table_removal.md` for full impact analysis, change list (~17 kernels + host), benefits, risks, and validation plan. Proposed as V6 Step 2' (replaces Step 2 identity-slot V fast path).
- Step 2' implementation smoke: removed slot-table indirection from CPU/CUDA/OpenCL and regenerated OpenCL embedded sources. Exporter/shape grep found no `slot_table` / `request_base` / `physicalSlot` dependency, so no export graph change is needed. Kept a request-level nonce (`PagedKVMeta::request_generation`) in CUDA/OpenCL async persistent-cache read keys; the removed mapping stays direct `slot = logical`.
- Step 2' sparse-row hardening: CPU/CUDA/OpenCL now explicitly validate active Q rows. `beginSparseQuery` requires strictly increasing logical indices, K/V write rejects full-Q sparse rows, and only the score layer may run full-Q/compact-output (`queryLen > activeLen`). Later `PicSparseAttention` layers must use compact Q/K/V rows (`queryLen == activeLen`) while kernels read K/V by `slot = logical` and pick Q row as `qLogical` only for score-layer full-Q.
- Step 2' build validation after hardening: OrangePi OpenCL/Vulkan, Jetson CUDA cross build, and Rhino/Aidlux Adreno OpenCL artifacts all built and passed `file`/CMake cache checks.
- Step 2' benchmark gate not accepted yet. MiniCPM5-1B ctx512 full-reuse after request-generation fix: Jetson `0.481185/0.472853s` vs `benchmark.csv` `0.313210591s` (best `1.51x` slower), OrangePi `0.624368/0.721490s` vs `0.429257s` (best `1.455x` slower), Rhino failed warm with `curl: (52) Empty reply from server` / SSH timeout log. Results were **not** merged into `benchmark.csv`. OrangePi profile smoke confirmed hydrate remained native direct async (`async_read=1 direct_segments=1 fallback_tokens=0`), so the regression is not explained by request-generation breaking prefetch.
- Step 2' root-cause attribution: Jetson full-reuse is slow because current server routing excludes `full-reuse` from graph-boundary and falls through to legacy split prefill (`seq_len=7` prefix + `seq_len=7` suffix), where dense graph/launch overhead dominates; CUDA hydrate+attention is only about `27.8ms`. OrangePi is different: non-detail profile already shows OpenCL hydrate `201.7ms` + attention `417.0ms`, and detail attribution points to `rearrange`, `qkv`, and `qk`. The existing Jetson `native-full-reuse-graph-boundary` 0.081s run is evidence against PagedCache/slot-table as the root cause, not the accepted fix target.
- Step 2' code cleanup: removed the legacy PIC split-forward chat path (`prefill_pic_prefix` -> external append -> sparse recompute -> `prefill_pic_suffix`) and the non-graph-boundary cacheblend score-prefill helper. `full-reuse` now requires the exported graph-boundary model with `pic_recompute_budget` and uses `native-full-reuse-graph-boundary`; non-graph-boundary PIC reuse/sparse modes fail early or fall back explicitly to full-compute metadata instead of silently using old schedulers.
- Step 2' Jetson runtime validation after syncing the rebuilt CUDA artifact: MiniCPM5-1B ctx512 `full-reuse` measured `0.080379s`, metadata reported `execution_mode=native-full-reuse-graph-boundary`, `recompute_token_count=0`, `reuse_token_count=498`, and the request log used `prefill_fixed_graph_external_pagedkv` with no legacy split/scorer stages.
- Step 2' current-artifact prefill regression check: rebuilt and synced the OrangePi OpenCL artifact after deleting the legacy split/suffix path, then ran MiniCPM5-1B ctx512 normal/full-reuse/cacheblend/epic against `benchmark.csv` rows only. Jetson shows no recompute regression versus `benchmark.csv` for cacheblend/epic, and path-deleted full-reuse is much faster (`0.080379s`, 0.257x benchmark). OrangePi still regresses versus `benchmark.csv` across full-reuse/cacheblend/epic, but normal full-compute also regresses (`2.329689s` vs `2.087799s`, 1.116x), and current-vs-old slot-removal smoke is mixed. Conclusion: OrangePi has a real current-artifact regression, but the available data does not isolate it to slot-table deletion; it points to an OpenCL/runtime/operator/tuning drift that also affects the normal baseline. No results were merged into `benchmark.csv`.
- OrangePi regression profile after path deletion: graph/debug profile shows the obvious regressions are not caused by hydrate or cacheblend score/top-k. `full-reuse` still pays layer0 full PagedAttention plus layer0/score-layer full-row Linear because `prefillFixedGraphExternalPagedKV()` runs one full prompt forward and only compacts at `score_layer_idx=1`; this is a graph-boundary floor, not slot-table cost. For MiniCPM5-1B ctx512 debug measure, `cacheblend 5%` spends about `1358 ms` in Convolution, `327 ms` in layer0 full PagedAttention, and `308 ms` in PicSparseAttention, while cacheblend score/top-k is only `2.3 ms` and hydrate `27 ms`. `cacheblend 30%` grows to about `2069 ms` Convolution and `530 ms` PicSparseAttention. Next root-cause work should focus on full-reuse layer0 boundary semantics, OpenCL compact-row Linear/MLP routing/tune cache, and cacheblend sparse-flash active logical distribution.
- Full-reuse graph-boundary floor resolved for MiniCPM5-1B ctx512 on OrangePi. `full-reuse` now uses a dedicated active-non-PIC prefill path: bind persistent PIC cache source for the whole logical prompt, activate sparse logical rows for prelude+suffix only, force non-scoring PagedAttention semantics, and feed a real `[active_rows, logical_length]` causal mask so the graph-boundary model's gather nodes still have valid shapes. The first two-forward hydrate+suffix attempt measured `0.930599s`; the active-row fix measures `0.406900s` versus `benchmark.csv` `0.429257s`. Metadata reports `execution_mode=native-full-reuse-hydrate-suffix`, `graph_boundary=none_full_reuse_hydrate_suffix`, `graph_level_boundary=false`, and `recompute_token_count=0`.
- Profile check for the fixed full-reuse path: exactly one `forwardRaw(seq_len=14)` for `prelude=7 + suffix=7`, no `cacheblend_score`, no `score_flash_attention`, no full-prompt `query=512` line, and profile latency `0.598636s` under profiling overhead. A manual `max_tokens=1` decode smoke returned HTTP 200 with the same no-boundary/no-recompute metadata.
- Cacheblend/epic after the full-reuse fix still regress on OrangePi MiniCPM5-1B ctx512 versus `benchmark.csv`: cacheblend is `1.209x-2.038x` slower and epic is `1.249x-1.469x` slower across 5%-50%. The run is mixed versus the earlier slotless sweep, has no old sparse path and no sparse layer 0/1 signal, and fixed full-reuse is now faster than benchmark, so the remaining regression is not attributable to the removed slot table or hydrate+suffix full-reuse. Existing profile attribution points to compact-row OpenCL Convolution/MLP, PicSparseAttention, and the required layer0/score-layer full compute floor; `cacheblend_score` and hydrate are small.

## Files

- `context.md`: implementation details, commands, A/B CSVs, logs, and highest-performance table.
- `claude_handoff_decode.md`: concise background, target, constraints, current state, and next actions for handoff.
- `decode_optimization_plan_v6.md`: unified Mali/Adreno decode plan — one transposed-K kernel family tuned across lane / q_tile / k_tile / gqa_shared / buffer-vs-image axes by device_family; no model/device routing; first steps are q=1 GQA-cooperative transposed kernel and Adreno image-backed decodeKey/value variants.
