interface:
  display_name: "MNN PIC Benchmark"
  short_description: "Run multi-device MNN PIC benchmarks"
  default_prompt: "Use $mnn-pic-benchmark from .codex/skills to deploy MNN PIC artifacts to Jetson, OrangePi, or Rhino and run per-device prefill, dataset, or decode benchmarks."
